# Copy to .env.local for local dev, or set these in your hosting/build environment.

# Google Analytics 4 measurement ID (format: G-XXXXXXXXXX).
# When unset, no analytics script loads — keeps dev/preview traffic out of the
# production property. Set this only in the production build environment.
NEXT_PUBLIC_GA_ID=
